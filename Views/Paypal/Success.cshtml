@model int
@{
    ViewData["Title"] = "Đặt hàng thành công";
}
<div class="container py-5">
    <div class="text-center">
        <h2 class="text-success mb-3">Thanh toán PayPal thành công 🎉</h2>
        <p class="mb-4">Mã đơn hàng của bạn: <strong>#@Model</strong></p>
        <a class="btn btn-primary me-2" href="@Url.Action("Index","Home")">Về trang chủ</a>
        <a class="btn btn-outline-secondary" href="@Url.Action("Functions_Details","Order", new { id = Model })">Xem chi tiết đơn</a>
    </div>
</div>
